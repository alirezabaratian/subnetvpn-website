<%- include('partials/header') %>
<div class="admin">
  <div class="title">لیست کاربران</div>
  <div class="table">
    <% users.forEach((user) => { %>
    <div class="user">
      <div class="text"><%= user.name %></div>
      <div class="text"><%= user.username %></div>
      <div class="text"><%= user.date %></div>
      <div class="text"><%= user.duration %></div>
      <div class="text"><%= user.pin %></div>
    </div>
    <% }); %>
  </div>
</div>
